# Importante: No modificar ni el nombre ni los argumetos que reciben las funciones, solo deben escribir
# codigo dentro de las funciones ya definidas.

def Factorial(numero):
    '''
    Esta funcion devuelve el factorial del numero pasado como parametro.
    En caso de que no sea de tipo entero y/o sea menor que 1, debe retornar nulo.
    Recibe un argumento:
        numero: Sera el numero con el que se calcule el factorial
    Ej:
        Factorial(4) debe retornar 24
        Factorial(-2) debe retornar nulo
    '''
    #Tu codigo aca:
    return 'Funcion incompleta'

def EsPrimo(valor):
    '''
    Esta funcion devuelve el valor booleano True si el numero reibido como parametro es primo, de lo 
    contrario devuelve False..
    En caso de que el parametro no sea de tipo entero debe retornar nulo.
    Recibe un argumento:
        valor: Sera el numero a evaluar
    Ej:
        EsPrimo(7) debe retornar True
        EsPrimo(8) debe retornar False
    '''
    #Tu codigo aca:
    return 'Funcion incompleta'
    
def ClaseAnimal(especie, color):
    '''
    Esta funcion devuelve un objeto instanciado de la clase Animal, 
    la cual debe tener los siguientes atributos:
        Edad    (Un valor de tipo de dato entero, que debe inicializarse en cero)
        Especie (Un valor de tipo de dato string)
        Color   (Un valor de tipo de dato string)
    y debe tener el siguiente metodo:
        CumplirAnios  (este metodo debe sumar uno al atributo Edad y debe devolver ese valor)
    Recibe dos argumento:
        especie: Dato que se asignara al atributo Especie del objeto de la clase Animal
        color: Dato que se asignara al atributo Color del objeto de la clase Animal
    Ej:
        a = ClaseAnimal('perro','blanco')
        a.CumpliAnios() -> debe devolver 1
        a.CumpliAnios() -> debe devolver 2
        a.CumpliAnios() -> debe devolver 3
    '''
    #Tu codigo aca:
    return 'Funcion incompleta'
